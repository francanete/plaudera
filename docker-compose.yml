services:
  # Local PostgreSQL for development
  db:
    image: pgvector/pgvector:pg17
    container_name: plaudera-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: plaudera
    ports:
      - "5432:5432"
    volumes:
      - plaudera_db_data:/var/lib/postgresql/data
      - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Optional: Full app container (uncomment if needed)
  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - .:/app
  #     - /app/node_modules
  #     - /app/.next
  #   env_file:
  #     - .env.local
  #   environment:
  #     - WATCHPACK_POLLING=true
  #     - CHOKIDAR_USEPOLLING=true
  #   depends_on:
  #     db:
  #       condition: service_healthy

volumes:
  plaudera_db_data:
